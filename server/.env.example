PORT=5000
CLIENT_URL=http://localhost:3000

# --- PostgreSQL ---
# Local example (after `createdb portfolio`):
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/portfolio

# Gmail example — use an "app password", not your real password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=you@gmail.com
SMTP_PASS=your_app_password
CONTACT_RECEIVER=hkrobin49@gmail.com

# --- Admin Panel Login ---
# Change these before deploying! This is the login used on the website's
# "Login" button in the navbar to unlock the editable Admin Dashboard.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=robin123
# Any long random string, used to sign login sessions.
JWT_SECRET=1611cf8c3d54313f541678d645cac6baea58d2dcc10529bba1c2c68dee81b31b
